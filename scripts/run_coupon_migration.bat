@echo off
REM 쿠폰 시스템 데이터베이스 마이그레이션 실행 스크립트 (Windows)

echo ============================================================
echo 쿠폰 시스템 데이터베이스 마이그레이션
echo ============================================================
echo.

REM 환경 변수 로드 (.env 파일이 있는 경우)
if exist .env (
    echo .env 파일에서 환경 변수 로드 중...
    for /f "tokens=*" %%a in (.env) do (
        set "%%a"
    )
)

REM Python 스크립트 실행
python scripts\run_coupon_migration.py

if %ERRORLEVEL% EQU 0 (
    echo.
    echo ============================================================
    echo 마이그레이션 완료!
    echo ============================================================
) else (
    echo.
    echo ============================================================
    echo 마이그레이션 실패. 오류를 확인하세요.
    echo ============================================================
)

pause
