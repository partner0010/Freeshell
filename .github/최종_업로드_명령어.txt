========================================
  🚀 최종 업로드 명령어 (순서대로!)
========================================

GitHub 페이지에 보이는 명령어와 함께
아래 순서대로 실행하세요!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  중요: 먼저 Git 사용자 정보를 설정해야 합니다!

1. Git 사용자 정보 설정 (필수!)

   git config --global user.email "partner0010@gmail.com"
   git config --global user.name "partner0010"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

2. 파일 추가

   git add .

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

3. 커밋 (이게 실패했던 이유!)

   git commit -m "Initial commit: Freeshell v2.0"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

4. GitHub 페이지에 나온 명령어 실행

   git remote add origin https://github.com/partner0010/freeshell.git
   git branch -M main
   git push -u origin main

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 한 번에 실행 (복사해서 붙여넣기):

git config --global user.email "partner0010@gmail.com"
git config --global user.name "partner0010"
git add .
git commit -m "Initial commit: Freeshell v2.0"
git remote add origin https://github.com/partner0010/freeshell.git
git branch -M main
git push -u origin main

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

⚠️  주의사항:

- git remote add origin 명령어는 이미 실행했다면
  "fatal: remote origin already exists" 에러가 나올 수 있습니다
- 그럴 경우 이 명령어는 건너뛰고 다음 명령어로 진행하세요

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 완료되면:

GitHub 페이지를 새로고침하면 파일들이 보일 것입니다!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

